## 📚 Глоссарий и правила БД

В этом разделе описаны основные типы данных, ограничения и правила целостности, используемые в проекте.

### Типы данных

*   `INT` — целое число.
*   `VARCHAR(n)` — строка переменной длины (до `n` символов).
*   `TEXT` — строка произвольной длины.
*   `ENUM` — строковый тип, который может принимать одно из заранее определенного списка значений.
*   `DATETIME` — хранит дату и время в формате `YYYY-MM-DD HH:MM:SS`.
*   `DECIMAL(10,2)` — число с фиксированной точностью, до 10 цифр, из которых 2 — после запятой.
*   `UNIQUE` — ограничение, гарантирующее уникальность значений в столбце или группе столбцов.

### Обязательность полей (`NOT NULL`)

*   **«Обязательный = Да»**: столбец имеет ограничение `NOT NULL`. Это означает, что при вставке или обновлении записи значение для этого столбца обязательно должно быть указано. Попытка записать `NULL` вызовет ошибку.
*   **«Обязательный = Нет»**: значение `NULL` в этом столбце допускается.

### Ограничения (`CONSTRAINTS`)

*   **`UNIQUE`** — гарантирует, что все значения в столбце (или группе столбцов) уникальны и не повторяются.
*   **`NOT NULL`** — запрещает сохранение `NULL`-значений в столбце.
*   **`DEFAULT`** — устанавливает значение по умолчанию для столбца, если при вставке новой записи значение не было указано явно.

### Правила удаления (`ON DELETE`)

Эти правила определяют, что произойдет с дочерними записями при удалении родительской.

*   **`RESTRICT` / `NO ACTION`** — **запрет**. Удалить родительскую запись не получится, пока существуют связанные с ней дочерние записи. Используется для критически важных данных (например, нельзя удалить роль, если есть пользователи с этой ролью).
*   **`CASCADE`** — **каскадное удаление**. При удалении родителя автоматически удаляются все связанные с ним дочерние записи. Применяется для зависимых сущностей (например, удаление группы влечет удаление записей о детях в этой группе).
*   **`SET NULL`** — **установка `NULL`**. При удалении родителя, внешний ключ в дочерних записях становится `NULL`. Используется, когда связь не является жесткой. *Важно: поле внешнего ключа должно быть `NULLABLE`.*
*   **`SET DEFAULT`** — **установка значения по умолчанию**. При удалении родителя, внешний ключ в дочерних записях получает значение по умолчанию, заданное через `DEFAULT`. Используется редко, например, для перевода записей на "запасного" родителя (например, "Система
